package ucf.assignments;

import static org.junit.jupiter.api.Assertions.*;

class ItemListTest {

    @org.junit.jupiter.api.Test
    void getComplete()
    {
        /*
           ->create a temporary ToDoListr arrayList
           ->add some itemList on that arrayList
           ->mark them as complete
           ->check if that changes the status from incompplete to complete
         */
    }
}